package Project.整合交通禮讓的MonteCarlo模擬系統;

public class TrafficSimulation extends MonteCarloSimulation {
    public TrafficSimulation(BaseSampling sampling, BaseReport report) {
        super(sampling, report);
    }
}

