package Project.整合樂透MonteCarlo模擬系統;

public class LottoSimulation extends MonteCarloSimulation {
    public LottoSimulation(BaseSampling sampling, BaseReport report) {
        super(sampling, report);
    }
}
